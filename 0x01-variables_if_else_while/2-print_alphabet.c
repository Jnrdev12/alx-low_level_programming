#include<stdio.h>
#include <ctype.h>
/**
 * main - Entry point
 * Description: to change lower acse to upper case
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	char str = tolower('H');

	printf("%c", str);
	return (0);
}
