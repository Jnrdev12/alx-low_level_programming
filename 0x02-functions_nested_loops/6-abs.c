#include "main.h"
/**
 * abs - Absolute value
 *
 * @a: Parameter whose absolute value needs to be found
 *
 * Return: a
 */

int _abs(int a)
{
	if (a < 0)
	{
		a = a * -1;
		return (a);
	}
	else
	{
		return (a);
	}
}
