#include "main.h"

/**
 *  * main - check the code
 *   *
 *    * Return: Always 0.
 *     */
int main(void)
{
	    char *str;

	        str = "012345 6789";
		    puts_half(str);
		    int r=strlen_noSpace(str);
		    _putchar(r);

		    return (0);
}
